# COMSOL From Data 2D

This example is a smoke/regression fixture for the existing regular-grid COMSOL
export wrapper. It is not a COMSOL faithful parity case.

Use `examples/comsol_faithful_2d` as the manifest-first template when the goal is
machine comparison against COMSOL release tables, field samples, force
inventory, and boundary events.
