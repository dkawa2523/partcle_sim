# Test fixtures

`legacy_*` directories are intentionally noncanonical v0.1 inputs used only to
verify `particle-tracer migrate`. They are not runnable examples and must not be
read by the production runtime.

User-facing, directly runnable v0.2 cases live under `examples/v02_*`.
